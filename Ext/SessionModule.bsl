
Процедура УстановкаПараметровСеанса(ТребуемыеПараметры)
	
	ТекущийИсполнитель = справочники.ФизическиеЛица.НайтиПоНаименованию(ИмяПользователя());
	Если ТекущийИсполнитель.Пустая() Тогда
	   Сообщение = Новый СообщениеПользователю;
	   Сообщение.Текст = "Не найден сотрудник по наименованию """ + ИмяПользователя() + """";
	   Сообщение.Сообщить(); 
	Иначе 	
	   ПараметрыСеанса.ТекущийПользователь = ТекущийИсполнитель;
	КонецЕсли;

КонецПроцедуры
